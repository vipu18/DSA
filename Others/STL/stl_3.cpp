#include <bits/stdc++.h>
#include <bitset>
using namespace std;

int main()

//bitset
/* {
    bitset <5> bt;
    cout << "enter your set: ";
    cin >> bt;
    cout << boolalpha; //gives true or false instead of integral values
    cout <<"Set is: "  << bt.all() << endl; //retrun true or false only when all bit are (1)
    cout << bt.any() << endl; //retrun true or false if 1 exist in any of bit
    cout << bt.count() << endl; //count number of bits
    //cout << bt.flip() << endl; //flip the set like 10111 to 01000
    cout << bt.none() << endl; //retrun true or false only when all bit are (0)
    //cout << bt.set(); //turns whole set to 1
    //cout << bt.set(2); //turns 2nd index to 1
    //cout << bt.set(2, 0); //turns 2nd index to 1
    //cout << bt.reset(); //turns element to 0
    cout << bt.reset(2) << endl; //turns 2nd element to 0
    cout << bt.size() << endl; //gives size of set 
    cout << bt.test(1); //check if the bit is a set or not
    return 0;
} */